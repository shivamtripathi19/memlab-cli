module.exports = function () {
  console.log(` 
  cm-cli [command] <options>
  path or -p [routerName]    .............. Find memory leak of start point to particular Route, where start point is login route.
  custom or -c [routerName]  .............. Find memory leak of particular Route.
  version or -v              .............. show package version.
  help or -h                 .............. show help menu for a command`);
};
